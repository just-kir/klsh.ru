
<%= form_for :quote, remote: true, url: quotes_path do |f| %>
  <tr>
    <td><%= quote.author %></td>
    <td><%= quote.text %></td>
    <td><%= link_to 'Show', quote_path(quote) %></td>
    <td><%= link_to 'Edit', edit_quote_path(quote) %></td>
    <td><%= link_to 'Destroy', quote_path(quote),
      method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
